# QA — Kimchi AI Editorial Portfolio

ตรวจสอบ 66 รายการผ่านทั้งหมดในสภาพแวดล้อมทดสอบ Chromium

## Responsive layout

| Viewport width | Document scroll width | ผล |
|---:|---:|---|
| 1920 px | 1920 px | ผ่าน |
| 1440 px | 1440 px | ผ่าน |
| 1024 px | 1024 px | ผ่าน |
| 768 px | 768 px | ผ่าน |
| 390 px | 390 px | ผ่าน |
| 360 px | 360 px | ผ่าน |
| 320 px | 320 px | ผ่าน |

ไม่มี horizontal overflow ของหน้าเว็บ แผนภาพมีการเลื่อนภายในกรอบของตัวเองบนหน้าจอเล็กโดยตั้งใจ

## ฟังก์ชันที่ตรวจ

- JavaScript เริ่มทำงานได้และไม่มี runtime exception ใน viewport ที่ทดสอบ
- Before / After, การเปลี่ยนภาพ, keyboard navigation ของ tabs
- ขยายภาพ, ปิดด้วย Esc และคืน focus ให้ปุ่มที่เรียก
- ตัวเลื่อนวัน 0, 7 และ 14 แสดง pH ตรงกับสมการ
- ค่าที่ส่งให้คำสั่ง Copy ของสมการและ Mermaid ตรงกับต้นฉบับ
- Mermaid โหลดไม่ได้ในโหมด offline แล้ว SVG สำรองยังคงอยู่
- เปิด / ปิด LaTeX Source และเทียบ source กับไฟล์จริง
- ปุ่มสไลด์ถัดไป, thumbnails, การวนกลับภาพแรก และ lightbox
- เมนูมือถือ, focus loop, Escape และการปลด scroll lock เมื่อเปลี่ยนขนาดจอ
- ปุ่ม GitHub ที่ยังไม่ตั้งค่าแสดงคำแนะนำ ไม่พาไป URL สมมติ
- reveal animation และโหมด reduced motion

ตรวจ path ของลิงก์และ asset ภายในทั้งหมดเทียบกับไฟล์จริง ตรวจว่ามี `.nojekyll`, h1 เดียว, alt text และ noopener สำหรับลิงก์เปิดแท็บใหม่

## ตรวจเอกสารและข้อมูล

ไฟล์ PDF, `.tex` และ Infographic ต้นฉบับตรงกับ SHA-256 ของไฟล์เดิม สำเนาที่ path `assets/pdf/` ตรงกันทุกไบต์ ค่าตาราง CSV 15 วันตรงกับแบบจำลองภายในความคลาดเคลื่อนจากการปัดทศนิยม

ไม่มีไฟล์ฟอนต์แนบในชุดเผยแพร่

## ขอบเขตและข้อจำกัดของการทดสอบ

เบราว์เซอร์ในสภาพแวดล้อมทดสอบมีนโยบายบล็อกการเปิด localhost และ file URL โดยตรง จึงทดสอบการเรนเดอร์และ UI ด้วยเอกสารในหน่วยความจำที่ใช้ CSS / JavaScript เดียวกัน และแทนที่ URL ของภาพ local ด้วย data URI เฉพาะในชุดทดสอบ ไม่ได้แก้ URL ในไฟล์ส่งมอบ

การทดสอบ Copy ตรวจ payload และ fallback command ไม่ใช่การอ่าน OS clipboard การทดสอบ Mermaid ครอบคลุม offline fallback; ไม่ได้ยืนยันการโหลด CDN จริงจากเครือข่ายนี้

ไม่ได้เผยแพร่ Repository หรือทดสอบ URL GitHub Pages จริงของผู้ใช้ ไม่ได้ทดสอบทุกเบราว์เซอร์หรืออุปกรณ์จริง หลังอัปโหลดควรเปิด URL ของตนและตรวจปุ่มดาวน์โหลดอีกครั้ง
